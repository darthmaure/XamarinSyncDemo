<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:d="http://xamarin.com/schemas/2014/forms/design"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:essentials="clr-namespace:Xamarin.Essentials;assembly=Xamarin.Essentials"
             mc:Ignorable="d"
             x:Class="FileSync.Views.AboutPage"
             xmlns:vm="clr-namespace:FileSync.ViewModels"
             Shell.NavBarHasShadow="False"
             IsBusy="{Binding IsBusy}">
    
    <ContentPage.BindingContext>
        <vm:AboutViewModel />
    </ContentPage.BindingContext>
    
    <ContentPage.Resources>
        <ResourceDictionary>
            <Color x:Key="LightTextColor">#999999</Color>
        </ResourceDictionary>
    </ContentPage.Resources>

    <StackLayout Padding="15">
        <Grid HeightRequest="160" BackgroundColor="{StaticResource NavigationPrimary}"
              Margin="-15,-15,-15,10">
            <Image Source="outline_lock_white_36dp.png" 
                   Style="{StaticResource LargeImageStyle}"/>
        </Grid>
        <ScrollView Grid.Row="1">
            <StackLayout Orientation="Vertical" Padding="16,40,16,40" Spacing="10">
                <Label FontSize="22" Text="FileSync" FontAttributes="Bold"/>
                <Label Text="0.0.0" TextColor="{StaticResource LightTextColor}"/>

                <Label Text="Simple application that can be used to share files from/to mobile device" />

                <Button Margin="0,10,0,0" 
                        Text="Logout"
                        Command="{Binding LogoutCommand}"
                        Style="{StaticResource ButtonStyle}" />
            </StackLayout>
        </ScrollView>
    </StackLayout>
    
</ContentPage>
